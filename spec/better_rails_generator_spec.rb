# frozen_string_literal: true

RSpec.describe BetterRailsGenerator do
  it "has a version number" do
    expect(BetterRailsGenerator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
